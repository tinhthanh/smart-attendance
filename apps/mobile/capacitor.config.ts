import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'asia.finos.smartattendance',
  appName: 'Smart Attendance',
  webDir: '../../dist/apps/mobile/browser',
};

export default config;
